import { Card, CardHeader, TableContainer, Box, TableHead, TableRow, TableCell, TableBody} from "@mui/material";

const GroupTable = (props) => {
    return (
        <Card>
            
        </Card>
    );
};

export default GroupTable